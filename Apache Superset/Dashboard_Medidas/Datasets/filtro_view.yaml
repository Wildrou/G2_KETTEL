- columns:
  - column_name: nombre
    type: VARCHAR(500)
  - column_name: pais_residencia
    type: VARCHAR(30)
  metrics:
  - expression: COUNT(*)
    metric_name: count
    metric_type: count
    verbose_name: COUNT(*)
  schema: dw_aero
  table_name: filtro_view
